import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sri Sai Collections",
  description:
    "Discover the essence of devotion with Sri Sai Collections. Explore our curated selection of spiritual treasures, from sacred artifacts to divine apparel. Embrace the grace of Sai Baba in every piece, crafted to inspire and uplift your soul. Shop now and experience the divine connection with Sri Sai Collections.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
